import { ImageDotDirective } from './image-dot.directive';

describe('ImageDotDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageDotDirective();
    expect(directive).toBeTruthy();
  });
});
